{
    "openapi": "3.0.1",
    "info": {
        "title": "Skill Sync LMS API",
        "version": "1.0.0",
        "description": "API documentation for Skill Sync"
    },
    "servers": [
        {
            "url": "http://localhost:3000",
            "description": "Local server"
        }
    ],
    "components": {
        "securitySchemes": {
            "bearerAuth": {
                "type": "http",
                "scheme": "bearer",
                "bearerFormat": "JWT",
                "description": "JWT Authorization header using the Bearer scheme."
            }
        }
    },
    "security": [
        {
            "bearerAuth": []
        }
    ],
    "paths": {
        "/api/users/login": {
            "get": {
                "summary": "Fetch controllers from the source",
                "description": "This is a placeholder for the actual API controllers. You can add paths dynamically based on your controllers.",
                "operationId": "getControllers",
                "responses": {
                    "200": {
                        "description": "Successfully fetched controllers"
                    }
                }
            },
            "put": {
              "description": "",
              "parameters": [],
              "responses": {
                "200": {
                  "description": "OK"
                }
              }
            }
        },
        "/api/users": {
          "get": {
            "description": "",
            "parameters": [],
            "responses": {
              "200": {
                "description": "OK"
              }
            }
          }
        }
    }
}
